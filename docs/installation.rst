.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install heron

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv heron
    $ pip install heron
